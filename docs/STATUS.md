# Состояние работы — 03.09.2026, 05:00 МСК

Точка входа для любого, кто продолжает: человек или агент. Обновлять при каждом заметном шаге.

## Что готово

- Репозиторий, окружение (`uv sync --group dev`), снимок курсов ЦБ 2015→2026-09-03 в `data/raw/`.
- Пакет `fxmoment`: ось публикации, шесть индикаторов (моментум, уровень, разворот, сезонность, провал относительно простой средней, обучаемый бустинг с гейтом уровня), разметка, метрики, walk-forward с калибровкой по медиане годовых lift и функцией среза, политика потока (ранг по усечённым исходам прошлых окон, отключение без lift или без выгоды, охлаждение с переносом через окна, шторм по волатильности), библиотека текстов с чекером, CLI (`fetch`, `backtest [--fixed-params]`, `analyze`, `signals --as-of [--decide]`, `check-texts`), отчёт с графиками, модуль анализа (цена ожидания, перенос параметров, без 2022, фронтир, достижимость 90/95/99 %).
- 33 теста зелёные: каузальность каждого индикатора, функция среза = полный прогон, калибровка и обучение не зависят от данных после `train_end`, ранг не зависит от текущего окна, перенос охлаждения через границу окна, шторм, чекер текстов.
- Независимый аудит кода агентом 03.09 (36 находок) — разобран: см. `docs/process/ai-workflow.md`; ADR 0003–0009 уточнены, ADR-0010 (внутридневной ряд Мосбиржи) принят.
- Отчёты: `reports/latest/` (калибровка walk-forward), `reports/latest/analysis/` (анализы), `reports/fixed/` (контроль: априорные параметры без калибровки).

## Что показал бэктест (11 окон 2021-01…2026-06, h = 20, допуск 25 бп, медианы по окнам; выгода — сверх случайного дня того же окна)

| Индикатор | lift «по среднему» по коридорам | Выгода, бп | Частота, в нед. | Вердикт |
|---|---|---|---|---|
| уровень (`level`) | 1,05–1,16 (pooled 1,00–1,27) | +39…+70 | 0,23–0,43 | скромно, но на всех коридорах ≥ 1; параметры переносимы между коридорами (`lift_drop` ≤ 0) |
| обучаемый (`ml_localmin`) | 0,89–1,49 (AMD 1,49, KGS 1,44) | +14…+97 | 0,08–0,23 | лучший по точности на 4 из 5, редкий; UZS < 1 |
| провал к тренду (`dip_vs_trend`) | 0,89–1,51 | −10…+155 | 0,11–0,42 | сильный на UZS/AMD/KGS, случайный на KZT; плохо переносится между коридорами |
| сезонность | 0,87–1,55 | −42…+112 | 0,08–0,11 | ≈ 4–6 событий в год на коридор, интервалы широкие; KGS < 1 |
| моментум | 0,95–1,08 | −11…+35 | 0,38–0,66 | не отличим от случайного дня — честный отрицательный результат |
| разворот (`WINDOW_CLOSING`) | 0,79–1,15 | −39…+73 | 0,31–0,46 | монетка на 4 из 5; без 2022 лучше (UZS 1,34) |

**Итоговый поток** (все сценарии, после политики и шторма): 0,39–0,61 пуша в неделю на коридор, пустых месяцев 8–30 %, самый длинный перерыв 65–168 дней, кучности нет; шторм снял 7–15 пушей на коридор за 5,5 лет. Точность `BUY_NOW`: lift 0,92–1,27, выгода +7…+70 бп.

**Фронтир «частота — точность»** (`analysis/frontier.png`): апостериорная верхняя граница внутри полосы 1–2 в неделю — lift 1,19–1,39 (+6…+124 бп), при < 0,5 в неделю — 1,23–1,57; рабочая точка калибровки — 1,00–1,27 при 0,3–0,55 в неделю. Точность покупается редкостью; полоса — потолок (ADR-0006).

**Цена ожидания** (`momentum` → `level`, k = 10): по подтверждённым парам курс за 2–4 дня ожидания падает ещё на 67–119 бп (интервалы целиком ниже нуля), по всем быстрым сигналам −17…+37 бп (интервалы через ноль). Стратегия «ждать подтверждения» лучше «слать сразу» на 5 из 5 коридоров (4 из 5 без 2022): выгода на отправленный пуш +14…+49 бп против −20…+4. Быстрый сигнал — режим наблюдения, не пуш.

**Уровни 90/95/99 %** (`analysis/tolerance_for_confidence.csv`): чтобы «не хуже 20 дней» выполнялось с 90 %, нужен допуск 710–900 бп — случайному дню нужен почти такой же. На дневном ряду ЦБ уровни из постановки недостижимы при содержательном допуске.

**Перенос параметров с KZT**: уровень переносится без потерь (перенесённый lift не ниже своего), провал и сезонность теряют 0,2–0,4 — параметры уровня общие, остальных — свои по коридору.

Главный вывод для концепции: на 1–5 днях ряд ЦБ почти непредсказуем, сигнал есть на горизонте месяца — это горизонт клиента, переводящего к зарплате. Быстрые индикаторы не дают ничего, «окно закрывается» — прогноз, а не факт. Пуш редкий (≈ 2 в месяц на коридор), факт о курсе на экране перевода — всегда.

## Открытые вопросы и следующие шаги (по приоритету)

1. **Промежуточные артефакты к 04.09 10:00** — `deliverables/interim/`: описание (обновить цифры), презентация (в сборке), продуктовые материалы, ссылка на репозиторий.
2. **Внутридневной ряд Мосбиржи** (ADR-0010): коннектор ISS, снимок, профиль движка в барах, прогон CNY и KZT; сверка «биржа против фиксинга ЦБ».
3. **Калибровка против априорных параметров** — сравнить `reports/fixed/` с `reports/latest/`; если калибровка не добавляет, продукт идёт на фиксированных параметрах с калибровкой как контролем.
4. **Стратифицированная база по месяцу** вместо полугодия — клиент переводит раз в месяц (`limitations.md`).
5. **Сезонность и налоговый период** — выгода по дням месяца, гипотеза о продаже валюты экспортёрами к 28-му.
6. **Признак EUR/USD** для ML (заменитель индекса доллара, из курсов ЦБ) — эксперимент.
7. Звёздочка: скриншоты пути в Web PWA и Альфа-Мобайл, компоненты `core-ds/core-components`, обзор практик (`docs/product/star-task-research.md` — собирается агентами), прототип.
8. Финал: отчёт, план пилота с частотой ≈ 0,5 в неделю, ограничения, репетиция со сценарием `fxmoment signals --as-of <дата> --decide`.

## Как воспроизвести

```bash
uv sync --group dev && uv run pytest && uv run fxmoment backtest && uv run fxmoment analyze
uv run fxmoment backtest --fixed-params
uv run fxmoment signals --as-of 2026-09-02 --decide --texts
```

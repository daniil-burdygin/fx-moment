# Состояние работы — 04.09.2026, 00:15 МСК

Точка входа для любого, кто продолжает: человек или агент. Обновлять при каждом заметном шаге.

## Что готово

- Репозиторий, окружение (`uv sync --group dev`), два снимка в `data/raw/`: дневные курсы ЦБ 2015→2026-09-03 и часовые свечи Мосбиржи 2018-01→2026-09-02 (54 858 баров, ADR-0010).
- Пакет `fxmoment` собран. Внутри ось публикации, шесть индикаторов (моментум, уровень, разворот, сезонность, провал относительно простой средней, обучаемый бустинг с гейтом уровня), разметка, метрики, walk-forward с калибровкой по медиане годовых lift и функцией среза, политика потока (ранг по усечённым исходам прошлых окон, отключение без lift или без выгоды, охлаждение с переносом через окна, шторм по волатильности), библиотека текстов с чекером, CLI (`fetch`, `fetch-moex`, `compare-sources`, `backtest [--fixed-params]`, `intraday`, `analyze`, `signals --as-of [--decide]`, `check-texts`), отчёт с графиками, модуль анализа (цена ожидания, перенос параметров, без 2022, фронтир, достижимость 90/95/99 %, помесячная база, форма месяца, календарное правило как база стека, выживаемость пуша до исполнения, разворот как сожаление; парные сравнения прогонов с интервалами по парам «коридор × окно» и по окнам).
- 105 тестов зелёные. Проверяются каузальность каждого индикатора, функция среза = полный прогон, калибровка и обучение не зависят от данных после `train_end`, ранг не зависит от текущего окна и считает по lift, а не по сырому hit, перенос охлаждения через границу окна, шторм, чекер текстов, разогрев каждого индикатора, живое окно после последнего теста, усечение исходов концом окна, ось внутридневного ряда и каузальность отбора по часу, масштабирование сеток профилем и совпадение дневного пути с прежним, отказ ранга при матрице чужой оси, разметка помесячной базы по сценарию индикатора, торговый день по началу бара, согласованный шаг изменений в сверке источников, порог сегмента пилота, который не сдвигается длительностью, причинность календарного правила, снятие постоянной части спреда при исполнении, пара «последний `BUY_NOW` → разворот» то, что интервал по окнам шире интервала по парам, совпадение строк общих окон у прогона с более раннего начала теста, месячная база в оценке событий и её откат на коротком месяце, ранг по столбцам месячной базы и таблицы сравнения вариантного прогона.
- Четыре волны проверки агентами 03.09: разбор кода (36 находок), проверка исправлений роем из 16 агентов, сверка чисел документов роем из 55 и аудит ночного диффа роем из 54 (47 подтверждённых находок, 8 критичных). Журнал и что отклонено — `docs/process/ai-workflow.md`. ADR 0003–0009 уточнены, ADR-0010 (внутридневной ряд Мосбиржи) и ADR-0011 (стратификация базы) заведены.
- Отчёты: `reports/latest/` (калибровка walk-forward), `reports/latest/analysis/` (анализы), `reports/fixed/` (контроль: априорные параметры без калибровки), `reports/intraday/` (сверка биржи с фиксингом); вариантные прогоны против `latest` — `reports/rank-month/`, `from2019/`, `ml-pooled/`, `level-drift/`, каждый с `vs_latest/`. Все собраны на одном коммите `main` (`7280800`) после слияния PR #2 и #3 в ночь на 04.09: числа прежние, CSV совпали побайтово.
- Доступ команды: `OSAndreev` (Олег) и `iFazaLove` (Илья) — коллабораторы с правом записи. Уклад на троих (ветка на задачу, кто пересобирает отчёты, `pull --rebase` перед push) — в `CLAUDE.md`; первое сообщение их агентам — `docs/process/onboarding.md`. Защиты `main` на бесплатном приватном репозитории нет, порядок держится договорённостью.

## Что показал бэктест (11 окон 2021-01…2026-06, h = 20, допуск 25 бп, медианы по окнам; выгода — сверх случайного дня того же окна)

| Индикатор | lift «по среднему» по коридорам | Выгода, бп | Частота, в нед. | Вердикт |
|---|---|---|---|---|
| уровень (`level`) | 1,05–1,16 (pooled 1,00–1,27) | +39…+70 | 0,23–0,43 | скромно, но на всех коридорах ≥ 1; параметры переносимы между коридорами (`lift_drop` ≤ 0) |
| обучаемый (`ml_localmin`) | 0,89–1,49 (AMD 1,49, KGS 1,44) | +14…+97 | 0,08–0,23 | лучший по точности на 4 из 5, редкий; UZS < 1 |
| провал к тренду (`dip_vs_trend`) | 0,89–1,51 | −10…+155 | 0,11–0,42 | сильный на UZS/AMD/KGS, случайный на KZT; плохо переносится между коридорами |
| сезонность | 0,87–1,44 | −13…+162 | 0,08–0,11 | ≈ 4–6 событий в год на коридор, интервалы широкие; KGS < 1 |
| моментум | 0,94–1,02 | −11…+21 | 0,46–0,77 | не отличим от случайного дня — честный отрицательный результат |
| разворот (`WINDOW_CLOSING`) | 0,79–1,15 | −39…+73 | 0,31–0,46 | монетка на 4 из 5; без окон 2022 — 0,99–1,34, выше 1 на четырёх коридорах |

**Итоговый поток** (все сценарии, после политики и шторма): 0,38–0,65 пуша в неделю на коридор, 735 пушей за 5,5 лет, пустых месяцев 8–27 %, самый длинный перерыв 80–168 дней, кучности нет; шторм снял 7–19 пушей на коридор. Точность `BUY_NOW`: lift 0,98–1,27, выгода +3…+57 бп.

**Фронтир «частота — точность»** (`analysis/frontier.png`): апостериорная верхняя граница внутри полосы 1–2 в неделю — lift 1,19–1,39 (+6…+124 бп), при < 0,5 в неделю — 1,23–1,57; рабочая точка калибровки — 1,00–1,27 при 0,32–0,55 в неделю. Точность покупается редкостью; полоса — потолок (ADR-0006).

**Цена ожидания** (`momentum` → `level`, k = 10): по подтверждённым парам курс за 2,5–3 дня ожидания падает ещё на 81–115 бп (интервалы целиком ниже нуля), по всем быстрым сигналам −22…+19 бп (интервалы через ноль). Стратегия «ждать подтверждения» лучше «слать сразу» на 5 из 5 коридоров (и без 2022): выгода на отправленный пуш +18…+52 бп против −24…+8. Интервалы двух стратегий перекрываются (`difference_within_ci`), поэтому это наклон, а не доказательство. Отсюда место моментума в ранге — последнее, и политика глушит его там, где на прошлых окнах у него нет lift: 484 срабатывания из 988 сняты как заглушённые, 239 всё же ушли пушем.

**Уровни 90/95/99 %** (`analysis/tolerance_for_confidence.csv`): чтобы «не хуже 20 дней» выполнялось с 90 %, нужен допуск 711–897 бп — случайному дню нужен почти такой же. На дневном ряду ЦБ уровни из постановки недостижимы при содержательном допуске.

**Перенос параметров с KZT**: уровень переносится без потерь (`lift_drop` −0,16…0,00), разворот тоже (−0,33…0,00); провал теряет 0,20–0,41 на трёх коридорах из пяти. У сезонности калибровка выбрала одну точку на всех коридорах, поэтому переносить нечего (`lift_drop` = 0).

**Калибровка против априорных параметров** (`reports/fixed/` — контрольный прогон без сетки; обучаемый индикатор в нём обучается как обычно, `--fixed-params` трогает только правила). Сетка не покупает точности (`analysis/calibration_vs_fixed.csv` — таблица считается кодом, раньше эти интервалы жили только здесь в тексте). Суммарно по всем правилам и коридорам pooled lift 1,041 на 3038 размеченных событий против 1,092 на 1420 у априорных параметров — разница 0,05 в пользу априорных при интервале [+0,00; +0,10]. Нижняя граница на нуле с точностью до третьего знака, то есть разница на грани различимости. По отдельным правилам калибровка выигрывает только на провале к тренду ([−0,29; −0,05] — априорная точка там хуже); на уровне ([+0,04; +0,24]), развороте ([+0,03; +0,34]) и сезонности ([+0,07; +0,50], но всего 39 событий и 1–10 молчащих окон из 11) лучше априорные; на моментуме разницы нет, у обучаемого её нет по построению. Сетка покупает объём: 2,1 × событий и 735 пушей потока против 545, частота 0,38–0,65 в неделю против 0,34–0,46. Механизм виден в условии допустимости — точка проходит при частоте 0,3–2,5 в неделю (у сезонности своя полоса 0,05–0,3 и минимум 12 событий вместо 30: при `rearm` = 20 общий порог ей недостижим), а априорные параметры срабатывают 0,04–0,35 раза в неделю, ниже полосы, и калибровка вынужденно уезжает в частый край сетки. Оговорка: априорные параметры и первый прогон лежат в одном коммите (`0bda0cf`) — они заданы до сетки, но не до данных, и контроль поэтому смещён в их пользу.

🧩 Что из этого следует. Продукт остаётся на калиброванных параметрах, но калибровка больше не выдаётся за источник точности: точность задают устройство индикатора и горизонт h = 20, сетка выбирает рабочую точку по оси частоты. Переводить продукт на априорные точки нельзя — они делают и без того тихий поток тише (545 пушей против 735), а сезонность на них вырождается: на UZS одно событие за 11 окон и 10 молчащих окон из 11. Уровень на априорных точках точнее при почти той же частоте (0,19–0,35 в неделю против 0,23–0,43) — это кандидат к финалу. Разворот тоже точнее, но втрое реже (0,08–0,15 против 0,31–0,46), то есть покупает точность редкостью, как и весь фронтир. Правкой сейчас ни то, ни другое не становится: сравнений было шесть, три значимых при смещённом контроле и общем интервале, упирающемся в ноль, основанием для перевода продукта не служат.

**Внутридневной ряд Мосбиржи** (ADR-0010, `reports/intraday/`; снимок 54 858 часовых баров с 2018-01-01). Коннектор ISS готов, сверка «биржа против фиксинга ЦБ» сделана — и первой же проверкой сузила задачу. Внутридневного рынка у трёх коридоров из пяти нет: у UZS сделки в 63 % рабочих дней по два бара в день, у KGS — по четыре, у TJS за два года 17 баров. Биржевая пара и кросс-курс ЦБ на этих коридорах — разные курсы: дневные изменения не связаны (R² ≈ 0), расхождение уровня 200–290 бп при медиане. У KZT и AMD рынок есть (9 баров в день), связь слабая (R² ≤ 0,35), расхождение 78 и 125 бп. На юане картина другая: связь сильнее всего в первой половине дня (медиана расхождения 11 бп, R² 0,74–0,80 к 10–12 часам) и к вечеру слабеет — расхождение растёт до 30 бп, R² падает до 0,32. Это согласуется с методикой ЦБ: курс дня считается по сделкам до 15:30 МСК (указание 6956-У), поэтому вечерние движения биржи в него уже не входят.

🧩 Что из этого следует. Внутридневной прогон имеет смысл на CNY, KZT и AMD; UZS, KGS и TJS остаются на дневном ряду, и это отрицательный результат в отчёт, а не пропуск. Второе: расхождение курса приложения с фиксингом теперь измерено, а не заявлено — на ликвидной паре медиана 31 бп и p90 107 бп за день, то есть больше рабочего допуска 25 бп. Механика «момент изменился» получает основание в цифрах. Оговорка о причинности: биржевая цена входит в расчёт курса ЦБ напрямую только там, где по валюте идут организованные торги; курсы коридорных валют ЦБ считает кросс-курсом через нацбанки (6956-У, п. 3.3), поэтому связь рядов там — общий драйвер, а не механизм.

**Прогон на часовой оси** (`reports/intraday/README_bars.md`; профиль в барах, 9 баров в торговом дне, горизонты 1 / 4 / 9 / 45 / 180 баров, сезонность и ML выключены). Дневной путь при этом не сдвинулся: все одиннадцать файлов `reports/latest` после параметризации побайтово совпали с прежними.

Внутридневного момента нет. На CNY — паре с самой длинной историей и 26 181 баром — lift «по среднему» лежит в 0,94–1,05 на горизонтах от часа до недели, выгода сверх случайного бара −3…+20 бп. На KZT разброс шире и вокруг единицы (0,71–1,11), на AMD тоже (0,77–1,20), но там всего семь окон. На месячном горизонте в барах (h = 180) pooled lift 0,84–1,15 против 0,89–1,27 на дневной оси у тех же четырёх индикаторов — точности переход на бары не покупает.

Частоты он тоже не покупает. Итоговый поток на часовой оси даёт медиану 0,00–0,50 пуша в неделю против 0,38–0,65 на дневной; в полосу 1–2 попадают 14–27 % окон против 0 %, но только за счёт отдельных всплесков — у CNY медиана нулевая, то есть больше половины окон молчат целиком. Пустых месяцев 16–74 % против 8–27 %, самый длинный перерыв 91–182 дня против 80–168. Точность потока не лучше дневной: `BUY_NOW` pooled lift 0,96–1,13 и выгода −32…+14 бп против 0,96–1,18 и +3…+57 на дневном ряду.

⚠️ Эти числа — исправленные. До аудита 03.09 здесь стояло «поток даёт 0,77–1,28 в неделю и попадает в полосу на 43–91 % окон». Причина в дефекте: `evaluate_stream` звала ранг индикаторов с дневным горизонтом h = 20, которого в матрице баров нет вовсе, срез выходил пустым, и весь слой ранга и отключений на часовой оси молча не работал. Поток шёл без единого отключения и давал вдвое больше пушей. Дефект закрыт, горизонт берётся у профиля, а матрица чужой оси теперь отвергается с ошибкой, а не молчанием.

🧩 Что из этого следует. Более частый ряд не даёт ни точности, ни пригодного потока: та же политика на часовой оси глушит так же, а сигналов, переживающих её, там не больше. Это снимает вопрос «а если брать данные почаще» — он проверен и закрыт отрицательно. Оговорки: у трёх коридоров из пяти внутридневного рынка нет вовсе, сравнение идёт по разным периодам (у KZT биржевые окна с 2022-12, у AMD с 2023-06), а CNY — не коридор, а эталон ликвидности.

**База: полугодие против месяца** (`analysis/monthly_baseline.csv`). Нынешняя база сравнивает сигнал со средним днём полугодового окна, а клиент, переводящий раз в месяц, выбирает день внутри месяца. Перешли на месячную базу — и часть выводов сдвинулась. Провал к тренду из «случайного на KZT» превращается в работающий на всех пяти коридорах: lift 1,13–1,35 вместо 0,92–1,18, выгода +30…+78 бп вместо −56…−5, и интервал разницы целиком выше нуля на всех пяти. Моментум поднимается с 0,89–1,04 до 1,05–1,12, но интервалы разницы через ноль на всех пяти коридорах. Уровень почти не двигается (1,00–1,27 против 1,01–1,18) — он устойчив к выбору базы; на AMD теряет (интервал −48…−1). Разворот на месячной базе, наоборот, уходит ниже единицы на четырёх коридорах из пяти (0,92–1,01 против 0,96–1,21) — он выигрывал у полугодовой базы за счёт выбора месяца. Сезонность почти не двигается.

🧩 Что из этого следует. Выбор базы двигает lift на величину до 0,28 — больше, чем большинство измеряемых нами эффектов. Это само по себе результат, и он идёт в отчёт: сравнивать надо не только индикаторы, но и то, с чем их сравнивают. Головной остаётся оконная база — она консервативнее и отвечает на вопрос «выиграл ли клиент за полугодие»; месячная стоит рядом и отвечает на вопрос «попали ли мы в удачный день внутри месяца», то есть на тот, который клиент решает на самом деле. Записано как ADR-0011, ждёт подтверждения Даниила. Там же неприятное следствие: ранг индикаторов и правило отключения считают lift на оконной базе, и за 11 окон провал к тренду отключён в 309 случаях из 496 — политика глушит на 62 % событий тот индикатор, который на клиентской базе работает везде. До сдачи не трогаем: правка ранга перестраивает весь поток и все отчёты. Механизм сдвига понятен: события провала к тренду кучкуются в месяцах, когда рубль слабеет, и оконная база штрафовала их за выбор месяца, а не за выбор дня.

**Сезонность и налоговый период** (`analysis/day_of_month.csv`, `day_of_month.png`). Гипотеза: экспортёры продают валюту под налоги к 28-му, рубль к концу месяца крепче, отправителю дешевле. Направление подтверждается на всех пяти коридорах: в окне 20–28 числа курс ниже среднего за свой месяц на 8–44 бп, а разница с остальными днями месяца — 12–63 бп. После снятия линейного тренда внутри месяца разница сжимается до 2,7–17,0 бп, но знак сохраняют все пять. Форма месяца читается: числа 6–14 дороже своей линии, 15–25 дешевле с провалом на 25-м, а 26–28 — резкий возврат вверх.

🧩 Что из этого следует. Механизм согласуется с данными, но не доказан. Коридоры скоррелированы с USD/RUB на 0,83–0,97, поэтому пять коридоров — почти одно наблюдение; интервал не пересекает ноль лишь у 11 клеток «коридор × число» из 155 при ожидаемых случайно восьми. Провал приходится на 15–25, а не на априорное окно 20–28, и переопределять окно под найденный провал мы не будем — это ровно та селекция, которую разбираем в других местах отчёта. Индикатор сезонности стреляет с 20–24-го числа, то есть попадает в провал, и это уже причинная проверка: он смотрит только на прошлые годы.

**Интервалы блоками по окну** (`analysis/calibration_vs_fixed.csv`, столбцы `*_by_window`; 03.09 вечер). Коридоры скоррелированы с USD/RUB на 0,83–0,97, а блок «коридор × окно» считает пять почти одинаковых коридоров пятью наблюдениями — интервал выходит уже, чем он есть. С блоком по окну (11 блоков) итоговая разница калибровки с априорными точками — те же 0,05, но интервал [−0,03; +0,14] содержит ноль; у разворота [−0,06; +0,47] тоже. Уровень ([+0,05; +0,25]), сезонность ([+0,05; +0,53]) и провал к тренду ([−0,34; −0,01]) вердикт сохраняют. Все парные сравнения отчёта теперь показывают оба интервала, и там, где они расходятся, верить надо более широкому.

**Календарное правило как база** (`analysis/calendar_rule.csv`, `calendar_vs_stack.csv`; 03.09 вечер). Правило без индикаторов, параметров и модели — «первый день публикации с 25-го числа». На 11 окнах оно даёт pooled lift 1,12 (по коридорам 1,06–1,18) и +71 бп сверх случайного дня (+60…+83) при 0,23 в неделю; «первый день с 20-го» — 1,08 и +31 бп. Стек это правило не обгоняет. Уровень (1,11, +44 бп) и обучаемый (1,16, +37 бп) от него не отличимы. Интервалы разницы содержат ноль и по lift, и по выгоде при обоих блоках. Поток хуже. `BUY_NOW` итогового потока (1,04, +34 бп) по выгоде уступает правилу «25-е» 36 бп при интервале [−55; −18] по парам и [−64; −11] по окнам — целиком ниже нуля в обоих. Провал к тренду хуже на 102 бп по парам ([−159; −43]), по окнам через ноль.

🧩 Что из этого следует. Календарь — не конкурент стека, а его нижний слой: он объясним за секунду, не требует ни данных, ни модели и уже даёт большую часть выгоды, которую мы меряем. Индикаторы обязаны доказывать прибавку над ним, а не над случайным днём, и сейчас доказать не могут. Для экрана перевода это готовый факт («в 2021–2026 первый день публикации с 25-го был не хуже среднего следующих 20 дней в 56 % случаев против 50 % у случайного дня»), для жюри — честная точка отсчёта. Окно правила задано до замера: провал формы месяца приходится на 15–25 числа, и переопределять правило под него мы не стали.

**Выживаемость сигнала до исполнения** (`analysis/execution_survival.csv`; 03.09 вечер). Пуш уходит вечером T, клиент переводит на T+1 по курсу приложения. Гейт уровня (`rank120 ≤ 0,2`), истинный вечером T, остаётся истинным по курсу T+1 у 87–98 % пушей `BUY_NOW` итогового потока (pooled 93 %), собственный факт индикатора — у 89–93 % (91 %). Спред: постоянная часть расхождения приложения с фиксингом на выбор дня не влияет и снята, считается отклонение сегодняшнего расхождения от среднего. На паре CNY за 2021–2026 (единственной, где биржа и фиксинг мерят один курс) среднее расхождение +14 бп, девятый дециль отклонения +74 бп. В ожидании по этому распределению выгода пуша не меняется (+29 → +30 бп pooled, попадание 0,53 → 0,55), но в девятом дециле уходит в минус: −45 бп по всем коридорам, от −37 до −90 на четырёх коридорах из пяти, AMD +5. На собственных парах KZT и AMD разброс втрое больше (девятый дециль +222…+247 бп), там выгода при неудачном спреде −140…−215 бп; это верхняя граница — эти пары с кросс-курсом ЦБ связаны слабо.

🧩 Что из этого следует. Девять пушей из десяти утром ещё верны, и это число для экрана: механика «момент изменился» нужна для десятого пуша и для хвоста спреда, а не для каждого. Хвост в десять процентов дней съедает всю выгоду сигнала — довод за механику A из `star-task.md` (честная дельта на экране), а не за фиксацию курса.

**Разворот как факт, не прогноз** (`analysis/reversal_regret.csv`; 03.09 вечер). Сценарий «окно закрывается» мерился как прогноз «через 20 дней курс не ниже» и был монеткой. Как факт для того, кто получил `BUY_NOW` и не перевёл, он выглядит иначе. По событиям индикаторов у каждого разворота есть `BUY_NOW` за 1–2 дня до него, и курс к развороту уже вырос. Медиана сожаления +41…+62 бп по коридорам (+54 pooled), среднее +81…+124 бп с интервалами целиком выше нуля на всех пяти; больше рабочего допуска 25 бп в 54–68 % пар. По пушам потока (81 пара из 90 разворотов, `BUY_NOW` в среднем за 5 дней до) медиана +33 бп, среднее +99 бп [+16; +184]. Прогнозные прочтения остаются у базы: через 20 дней lift 0,96–1,21 (pooled 1,08), до конца календарного месяца 0,97–1,31 (1,07).

🧩 Что из этого следует. Роль сценария — не «курс будет выше», а «после подсказки прошло пять дней, курс уже выше на столько-то»: факт о прошлом, честный для клиента, который не перевёл, и не требующий прогноза. Текст пуша не меняется — он и так называет только прошлое; роль сценария переписана в `concept.md`.

**Ранг на месячной базе** (вариант `reports/rank-month/`, сравнение в `vs_latest/`; 03.09 вечер). Кандидат из «Следствий» ADR-0011 замерен: ранг и отключение индикаторов считаются по месячной базе, всё остальное то же. Матрица совпала побайтово (6600 строк), сдвинулись только решения политики. Провал к тренду перестаёт глушиться (309 отключений → 0, отправлено 54 → 140), моментум почти перестаёт (484 → 82, отправлено 239 → 409), разворот глушится чаще (295 → 347). Поток чаще: 735 → 934 пушей, медиана 0,57–0,72 в неделю, пустых месяцев 0–12 % вместо 8–27 %. И хуже: `BUY_NOW` lift 1,04 → 0,99, выгода сверх случайного дня +34 → +15 бп. Разница −0,05 [−0,09; −0,01] и −19 бп [−30; −9] по парам, [−0,08; −0,01] и [−32; −8] по окнам — целиком ниже нуля в обоих блоках. Без базы то же: доля попаданий пушей 0,53 → 0,52, выгода вперёд самих пушей +29 → +21 бп (`stream_raw.csv`).

🧩 Что из этого следует. Месячная база прощает индикатору выбор месяца, и в поток идут моментум и провал к тренду, которые на оконной базе глушатся; чаще — да, точнее — нет. Рекомендация: ADR-0011 оставить как есть (головная база — окно), кандидат отклонить с этим замером. Решение — Даниила, строка в `docs/plan.md`.

**Тест с 2019** (вариант `reports/from2019/`; 03.09 вечер). 15 окон 2019-01…2026-06 вместо 11. На 11 общих окнах матрица совпала побайтово (6600 строк, 33 столбца, 0 расхождений) — параметризация ничего не сдвинула. Окна 2019–2020 обучаются от 2018, у первого окна один год: калибровка допустима у 20 % правил, дальше 76–96 %. Индикаторы на четырёх ранних окнах: уровень pooled 1,16 (+21 бп), провал к тренду 1,32 (+34), обучаемый 1,19 (+30), сезонность 1,04, моментум 0,92 (−21) — картина 2021+ держится; разворот как прогноз тогда выглядел лучше, чем потом (1,38, +32 бп). 2020 — режим: уровень 1,51 (+107 бп на 26 событиях) в первом полугодии и 0,75 (−47 бп) во втором. Поток на 2019–2020 слаб: `BUY_NOW` 267 событий, lift 0,94, −14 бп (второе полугодие 2020 — 0,74): политике не хватает истории окон, а шоковое полугодие она не проходит. На общих окнах поток тоже сдвинулся. Ранг читает всю историю окон, и с 2019–2020 в ней моментум глушится почти целиком (отправлено 239 → 58), а уровень пропускается чаще (193 → 250). Пушей 735 → 598, у TJS 159 → 82 с пустыми месяцами 8 % → 45 %. `BUY_NOW` lift 1,04 → 1,03, выгода +34 → +21 бп, интервалы через ноль в обоих блоках. Выгода вперёд самих пушей +29 → −6 бп, и вся разница сидит в окне 2022-H1 (−60 бп сверх случайного дня против +137 у `latest`); на остальных десяти окнах вариант лучше на восьми и хуже на двух (−11 и −5 бп).

🧩 Что из этого следует. Выводы по индикаторам от начала теста не зависят, головной протокол (окна с 2021, ADR-0004) остаётся; прогон с 2019 — приложение о чувствительности. Поток от длинной истории ранга не выигрывает: история тянет режим 2019–2020 в решения 2022 года. Кандидат — ограничить историю ранга последними окнами; не реализован, место ему на финальном прогоне.

**Общий обучаемый на пять коридоров** (вариант `reports/ml-pooled/`, сравнение в `vs_latest/`; 03.09, 23:40). Тот же бустинг обучается на строках всех пяти коридоров до даты среза с признаком коридора (в обучении 2825–7770 строк, впятеро больше), порог по-прежнему на коридор. Пять базовых индикаторов совпали с `latest` побайтово (5500 строк), разница изолирована в обучаемом. Суммарно pooled lift 1,156 → 1,188 (+0,032 при интервалах [−0,03; +0,09] по парам и [−0,04; +0,11] по окнам), выгода сверх случайного дня +37,0 → +38,5 бп ([−15; +18] / [−11; +18]) — всё через ноль. По коридорам лучше по lift на четырёх из пяти (KGS 1,28 → 1,33, KZT 1,09 → 1,13, TJS 1,10 → 1,12, UZS 0,98 → 1,07 — впервые выше единицы), хуже на AMD (1,44 → 1,31); все интервалы по коридорам содержат ноль. Пороги те же, пустых фильтров 7 окон из 55 в обоих. Поток: 735 → 744 пуша, `BUY_NOW` lift 1,041 → 1,037, выгода +34,3 → +33,8 бп — через ноль; без базы hit 0,532 → 0,527. Разбор — `docs/decisions/pooled-ml-experiment.md`.

🧩 Что из этого следует. Общее обучение даёт ту же картину, что признак EUR/USD (1,156 → 1,190 через ноль): направление верное на четырёх коридорах, UZS переходит единицу, но эффект от нуля не отличим — пять коридоров близки к одному наблюдению. Первая строка матрицы обучаемого есть; «общий + EUR/USD» против базового — на финальном прогоне 05.09 тем же `compare-runs`. В `latest` не переносить: сдвинулись бы все числа обучаемого в документах ради эффекта через ноль.

**Уровень с вычтенным дрейфом локальной ноги** (вариант `reports/level-drift/`, пара `level_drift → level` в `vs_latest/pairs_compare.csv`; 03.09, 23:40). `level_drift` считает процентиль среди значений окна, приведённых к дрейфу локальной ноги `rate / usd` за скользящий год; стоит рядом с `level`, сетка та же. Шесть прежних индикаторов совпали с `latest` побайтово (6600 строк). Против `level` суммарно: 623 → 682 события, pooled lift 1,107 → 1,141 (+0,03 при интервалах [−0,05; +0,12] в обоих блоках), выгода +44,0 → +36,5 бп (−7,5, [−35; +19] / [−38; +19]) — через ноль. По коридорам картина разная. KGS: lift 1,00 → 1,14 при интервале [+0,03; +0,34] целиком выше нуля, выгода +18 → +43 бп через ноль. UZS: 1,06 → 1,21 ([−0,02; +0,33]), выгода 38 → 38. KZT: 1,09 → 0,95 и +30 → −23 бп при обоих интервалах целиком ниже нуля ([−0,36; −0,01], [−121; −5]). AMD и TJS без разницы. Медианный дрейф в фактах: KZT −6,4 % в год, UZS −2,7 %, AMD +2,5 %, TJS +2,6 %, KGS +0,7 %. От 64 до 86 % его событий лежат в трёх днях от события `level`. Перенос параметров с KZT: у `level` `lift_drop` −0,16…0,00, у `level_drift` +0,14…+0,17 на KGS, TJS и UZS — переносится хуже. Политика глушит его в 231 событии из 682, пропускает 137; поток 735 → 787 пушей (KGS +17, TJS +16, UZS +14), `BUY_NOW` lift 1,041 → 1,048, выгода +34,3 → +33,4 бп — через ноль; без базы hit 0,532 → 0,535. Разбор — `docs/decisions/level-drift-experiment.md`.

🧩 Что из этого следует. Поправка на дрейф работает там, где дрейф ровный (UZS: 2024–2025 lift 1,25–1,95 против 0,75–0,89 у `level`), и ломается там, где локальная нога дешевеет ступенями (KZT: 2022-H1, 2024-H1, 2025-H1 — годовой дрейф отстаёт от шага). Правило устойчивости ADR-0004 требует эффекта на нескольких коридорах — его нет, и в стек `level_drift` не идёт: поток громче на 52 пуша без прибавки точности, параметры переносятся хуже, чем у `level`. Дрейф остаётся фактом для экрана и текста («курс сума к доллару за год снизился на 2,7 %») и приложением к отчёту с разбором по коридорам. Поправку Ильи на дрейф UZS, если она сформулирована иначе, сверять этой же парой, а не заводить второй вариант. Решение — Даниила.

Главный вывод для концепции: на 1–5 днях ряд ЦБ почти непредсказуем, сигнал есть на горизонте месяца — это горизонт клиента, переводящего к зарплате. Быстрые индикаторы не дают ничего; календарное правило без индикаторов даёт большую часть выгоды, и стек обязан доказывать прибавку над ним; «окно закрывается» — факт о сожалении, а не прогноз. Пуш редкий (≈ 2 в месяц на коридор), факт о курсе на экране перевода — всегда. Ранг на месячной базе и длинная история ранга поток не улучшают — замерено вариантными прогонами.

## Открытые вопросы и следующие шаги (по приоритету)

1. **Промежуточные артефакты к 04.09 10:00** — `deliverables/interim/`: описание и продуктовые материалы сверены с отчётами после аудита, осталась презентация.
2. ✅ **Внутридневной ряд Мосбиржи** (ADR-0010) закрыт: коннектор, снимок, сверка с фиксингом, профиль движка в барах и прогон по CNY, KZT и AMD. Ответ отрицательный и по точности, и по частоте — выше по тексту.
3. ✅ **Стратифицированная база по месяцу** и **сезонность против налогового периода** закрыты — выше по тексту.
4. ✅ **Признак EUR/USD** для ML замерен — ветка `feat/eurusd-feature`, разбор в `docs/decisions/eurusd-experiment.md`. Pooled lift обучаемого 1,156 → 1,190, выгода сверх случайного дня +37,0 → +42,4 бп, лучше на четырёх коридорах из пяти по обеим метрикам, UZS впервые переходит единицу. Интервалы парного блочного бутстрепа [−0,025; +0,103] и [−12,6; +22,3] — оба содержат ноль. Остальные индикаторы не сдвинулись, поток 735 → 731 пуш. 🧩 Не мержим до сдачи: правка всех чисел обучаемого ради эффекта, интервал которого содержит ноль. Решение — Даниила, место ему на финальном прогоне 05.09.
5. Звёздочка: обзор практик собран (`docs/product/star-task-research.md` — 51 кейс с цитатами первоисточников); осталось снять скриншоты пути в Web PWA и Альфа-Мобайл и собрать прототип на компонентах `core-ds/core-components`.
6. Финал: отчёт, план пилота с частотой ≈ 0,5 в неделю, ограничения, репетиция со сценарием `fxmoment signals --as-of <дата> --decide`. Сценарий прогнан 03.09 и повторно после правок аудита; на 2026-09-02 все коридоры молчат, поэтому для показа брать **2026-06-22** — в этот день политика отправляет два пуша (KGS по уровню, AMD по сезонности). `fxmoment check-texts` чист.
7. Решить по ADR-0011: ранг на месячной базе замерен вариантом `reports/rank-month/` — поток чаще и хуже при интервалах целиком ниже нуля в обоих блоках (выше по тексту). 🧩 Рекомендация — оставить оконную базу, тогда числа потока в документах не меняются. Решение — Даниила.
8. Вариантные прогоны к финалу (💬 03.09 вечер, `docs/plan.md`): ✅ все четыре замерены — ранг на месячной базе, тест с 2019, общий обучаемый на пять коридоров, уровень с вычтенным дрейфом локальной ноги — выше по тексту, механика `backtest --out`, `compare-runs --pair`. Ждут слова Даниила: `level_drift` (🧩 в стек не брать, дрейф оставить фактом) и слот обучаемого (🧩 решать по всей матрице «базовый · +EUR/USD · +TimesFM · общий · общий +EUR/USD» на финальном прогоне 05.09; в `latest` пока ничего не переносить). Четыре добавляющих пункта (календарное правило, интервалы по окнам, выживаемость, сожаление) тоже закрыты — выше по тексту.

## Как воспроизвести

```bash
uv sync --group dev && uv run pytest && uv run fxmoment backtest
uv run fxmoment backtest --fixed-params   # до analyze: сравнение калибровки читает его матрицу
uv run fxmoment analyze
uv run fxmoment signals --as-of 2026-09-02 --decide --texts
uv run fxmoment fetch-moex && uv run fxmoment compare-sources && uv run fxmoment intraday
uv run fxmoment backtest --rank-base month --out rank-month && uv run fxmoment compare-runs --variant rank-month
uv run fxmoment backtest --first-test 2019-01-01 --out from2019 && uv run fxmoment compare-runs --variant from2019
uv run fxmoment backtest --ml pooled --out ml-pooled && uv run fxmoment compare-runs --variant ml-pooled --pair ml_localmin=ml_localmin
uv run fxmoment backtest --with-level-drift --out level-drift && uv run fxmoment compare-runs --variant level-drift --pair level_drift=level
```

Порядок важен: `analyze` читает `reports/latest` и `reports/fixed`, `intraday` — матрицу
`reports/latest`, поэтому все пять отчётов собираются подряд на одном коммите. Провенанс каждого без суффикса `-dirty` —
проверка того, что так и вышло. Вариантные прогоны — после них. Каждый пишет в свой каталог и
`reports/latest` не трогает, а `compare-runs` читает оба.

# Состояние работы — 03.09.2026, 04:10 МСК

Точка входа для любого, кто продолжает: человек или агент. Обновлять при каждом заметном шаге.

## Что готово

- Репозиторий, окружение (`uv sync --group dev`), снимок курсов ЦБ 2015→2026-09-03 в `data/raw/`.
- Пакет `fxmoment`: ось публикации, шесть индикаторов (моментум, уровень, разворот, сезонность, провал относительно тренда, обучаемый бустинг с гейтом), разметка, метрики, walk-forward с калибровкой и функцией среза, политика потока с walk-forward-отключением слабых индикаторов, библиотека текстов с чекером, CLI, отчёт с графиками.
- 25 тестов зелёные, в том числе `tests/test_no_lookahead.py`: состояние на дату среза совпадает с полным прогоном для правил и для ML.
- Первый полный бэктест: `reports/latest/` (матрица, события, решения политики, сводки, графики по коридорам).

## Что показал бэктест (12 окон 2021-01…2026-09, допуск 25 бп, горизонт 20 дней публикации)

| Индикатор | lift «по среднему», медиана по коридорам | Выгода сверх случайного дня, бп | Вердикт |
|---|---|---|---|
| уровень (`level`) | 1,06–1,34 | +41…+105 | работает на месячном горизонте; на 5 днях ≈ 1,0 |
| сезонность | 1,10–1,46 | +68…+231 | сильно, но ≈ 4 события в год на коридор — широкие интервалы |
| обучаемый (`ml_localmin`) | 0,98–1,37 | 0…+86 | умеренно; пока не лучше уровня |
| провал к тренду (`dip_vs_trend`) | 0,84–1,15 | −63…+74 | нестабилен, на AMD и TJS хуже случайного дня |
| моментум | 0,86–1,06 | −10…+36 | не отличим от случайного дня — честный отрицательный результат |
| разворот (`WINDOW_CLOSING`) | 0,93–1,06 | −21…+13 | монетка; политика глушит его walk-forward почти во всех окнах |

Итоговый поток `BUY_NOW` после политики: 0,24–0,44 пуша в неделю на коридор (ниже полосы 1–2), lift «по среднему» медиана 0,84–1,50, пустых месяцев 10–37 %, самый длинный перерыв до 159 дней. Кучности нет.

Главный вывод для концепции: на 1–5 днях ряд ЦБ почти непредсказуем, сигнал есть на горизонте месяца — это горизонт клиента, переводящего к зарплате. Быстрые индикаторы не дают ничего, «окно закрывается» — прогноз, а не факт.

## Открытые вопросы и следующие шаги (по приоритету)

1. **Частота потока ниже полосы.** Варианты: расширить сетку уровня (pct до 0,4, окна 40–60), добавить внутридневной ряд MOEX как второй источник, ослабить охлаждение. Решение принять после разговора с кейсодателем о тихих периодах (ответ: молчание — допущение команды).
2. **Сезонность** — разобрать, какие месяцы дают эффект (ожидаемо декабрь/январь), и проверить устойчивость без 2022.
3. **Цена ожидания** — посчитать `metrics.price_of_waiting` между моментумом (быстрый) и уровнем (медленный) по коридорам; это пункт 6 постановки, в отчёте пока нет.
4. **Перенос параметров между коридорами** (ADR-0004, п. 3) — второй прогон с параметрами KZT на всех коридорах; кода пока нет.
5. **Отчёт**: фронтир «частота — точность» (ADR-0008), таблица с 2022 и без, три уровня вероятности; в `stream_summary` привести имена столбцов к `hit_mean/lift_mean`.
6. **Промежуточные артефакты к 04.09 10:00** — `deliverables/interim/`: описание в Markdown, продуктовые материалы, презентация; черновиков пока нет.
7. Звёздочка — не начата; нужен участник с Альфа-Мобайл и настроенным переводом.

## Как воспроизвести

```bash
uv sync --group dev && uv run pytest && uv run fxmoment backtest
uv run fxmoment signals --as-of 2026-09-02 --only-signals --texts
```

# Прототипы и демо

- `timemachine.html` — машина времени: сигналы «как на дату T» (строка Ш9 плана). Собирается из
  `reports/latest` командой `uv run fxmoment timemachine`. Открывается офлайн. Ряды, решения политики
  и тексты — из отчёта, вердикты попаданий — теми же функциями `labels`, что в бэктесте; скрипт
  страницы только скрывает всё после T и по кнопке раскрывает следующие 20 дней публикации. Журнал
  попаданий на дату T считает лишь пуши, чей горизонт закрылся не позже T. В подвале — хеш кода
  прогона и снимок данных: после пересборки отчётов страницу пересобрать той же командой, иначе она
  показывает прежний прогон.
- Живая проверка среза на защите — `uv run fxmoment signals --as-of <дата> --decide --texts`. На
  2026-06-22 команда работает 10 с (замер 04.09.2026 на ноутбуке, CPU) и считает состояние
  индикаторов и решение политики заново. Страницу она не читает.
- `index.html` — экран «момент изменился» и журнал попаданий, строки Ш3 и Ш10. Разбор ниже.
- `journey.html` — кликабельный клиентский путь из пуша в перевод на визуальном языке Альфа-Онлайн,
  с развилкой «сигнал устарел». Разбор в конце файла.

## `index.html` — «момент изменился» и журнал попаданий

Один файл, CSS и JS внутри, ни одного внешнего запроса: открывается двойным щелчком без сервера и
без сети. Рамка повторяет интерактивный концепт команды — слева заголовок шага, в центре телефон
390 × 844, справа панель «Действия», внизу дисклеймер.

Четыре состояния переключаются панелью справа, клавишами 1–4 и параметром адреса:
`index.html?state=actual` · `changed` · `closed` · `journal`; `&how=1` открывает страницу с уже
раскрытым «Как рассчитано».

| Состояние | Что показывает | Механика |
|---|---|---|
| `actual` | пуш открыт вечером 09.06.2026: предзаполненная форма, курс a_T, факт с окном, «Как рассчитано» | предзаполнение (`star-task.md`, «Предзаполнение формы») |
| `changed` | тот же пуш открыт 22.06.2026: две цифры, дельта в сомони и рублях, форма сохранена | A, «честная дельта» |
| `closed` | тот же пуш открыт 01.07.2026: факт больше не выполняется, экран говорит это первой строкой | A в худшем сценарии |
| `journal` | последние 12 пушей коридора с вердиктом «сбылось / не сбылось / ещё идёт» | Ш10, «пуш, который сам себя оценивает» |

### Откуда числа

Придуманы только трое: получатель, банк и назначение — клиентских данных в проекте нет. Всё
остальное на странице взято из снимка.

- **Пуш** — `reports/latest/stream_decisions.csv`, коридор TJS, `decision=sent`, 09.06.2026:
  индикатор `level`, курс действия 7,66882 ₽ за 1 сомони, `pct_rank` 0,0417, окно 120 дней
  публикации, снижение остановилось 14 дней назад. Хеш кода прогона — в `reports/latest/provenance.json`
  рядом с CSV, а не переписан сюда: пересборка отчётов его меняет.
- **Заголовок и текст уведомления** — `fxmoment.texts.library.render`, шаблон `level_stall`
  (при `days_since_min ≥ 3` библиотека сама выбирает его вместо `level`).
- **Курсы на даты открытия** — `data/raw/cbr_daily.csv`, снимок ЦБ РФ 03.09.2026:
  22.06.2026 — 7,93983 ₽ (+3,5 %), 01.07.2026 — 8,43411 ₽ (+10,0 %). Процентили в окне 120 дней
  публикации: 0,2833 и 0,8667 — отсюда «ниже, чем в 72 % дней» и «выше, чем в 87 % дней».
- **Сводка потока** в подвале журнала — `reports/latest/README.md`, строка `TJS · BUY_NOW`:
  `hit_mean_pooled` 0,531 против `base_mean_pooled` 0,551.

Расчёт получателя идёт по официальному курсу ЦБ РФ, без комиссии и спреда банка; на экране это
сказано, в дисклеймере — тоже.

### Как пересчитать журнал

Вердикт считает сама страница: `verdict()` в `index.html` сравнивает `mean20` с
`rate × (1 − 25 / 10⁴)` — это `labels.hit_buy_now(..., mode="mean")` из ADR-0003. «Ещё идёт» стоит
там, где горизонт не закрылся на дату журнала (22.06.2026), и `mean20` в данных равен `null`:
задним числом вердикт не подставляется.

Пересобрать массив `JOURNAL` после нового прогона:

```python
import json, pandas as pd
from fxmoment.data.calendar import to_publication_panel
from fxmoment import labels

rate = to_publication_panel(pd.read_csv("data/raw/cbr_daily.csv"))["TJS"].dropna()
mean20 = labels.future_mean(rate, 20)
dec = pd.read_csv("reports/latest/stream_decisions.csv")
sent = dec[(dec.corridor == "TJS") & (dec.decision == "sent")].tail(12)
as_of = pd.Timestamp("2026-06-22")
for _, row in sent.iterrows():
    t = pd.Timestamp(row["date"])
    pos = rate.index.get_loc(t)
    known = rate.index[pos + 20]          # день, когда вердикт стал известен
    print(t.date(), row["rate"], None if known > as_of else round(mean20[t], 5), known.date())
```

Дата журнала выбрана той же, что у состояния `changed` и у сквозного сценария защиты
(`signals --as-of 2026-06-22`), чтобы все четыре экрана рассказывали одну историю.

### Токены дизайна

Взяты из открытой дизайн-системы Альфа-Банка `core-ds/core-components` **на глаз**: сети в сессии
нет, npm не подключён, значения выписаны в `:root` файла руками. Точен один акцентный красный
`#EF3124`. Скругления 16–20 px, тёмная подложка, системный шрифт и размеры — приближение, которое
при подключении пакета заменяется его переменными; иконок и шрифтов из пакета не берётся ничего,
всё нарисовано CSS, а флаг — эмодзи системного шрифта.

### Что здесь 🧩, а не решение

- Даты открытия (22.06 и 01.07), сумма 10 000 ₽, получатель, банк и назначение — выбор агента.
- «Извините, сигнал устарел» противоречит пункту 20 `star-task-research.md` («экран не извиняется»).
  Формулировка взята из постановки задачи; если побеждает пункт 20, первую строку состояния
  `closed` надо переписать на факт без извинения.
- Порог экрана расхождения 25 бп взят из рабочего допуска словаря; по коридорам он не разведён.
- Вторая кнопка названа «Уведомлять о новом сигнале», а не «Напомнить, когда курс вернётся»:
  «вернётся» — будущее время, его снимает чекер `fxmoment check-texts`.

## `journey.html` — путь из пуша в перевод

Один файл, CSS и JS внутри. Наружу он ходит только за оформлением банка: три `@font-face` на шрифт
(`alfabank.servicecdn.ru/media/fonts/alfa-interface-sans_{regular,medium,bold}.woff2`, `font-display: swap`)
и три иллюстрации способов перевода с того же CDN. Ни шрифт, ни картинки в репозиторий не копируются:
офлайн страница падает на системный фолбэк той же метрики, место иллюстрации остаётся пустым,
и файл открывается двойным щелчком. Рамка та же, что у `index.html`: слева заголовок шага, в центре
телефон 390 × 844, справа панель «Действия» с кнопками текущего шага, внизу дисклеймер.

Путь проходится внутри телефона: тап по уведомлению открывает перевод, кнопки экранов ведут по
шагам, круглая кнопка шапки возвращает назад, карточка способа выбирается как радио, лист назначения
подставляет выбранный пункт, клавиатура правит сумму (пустая сумма гасит «Продолжить»), а строка
«Открыть как» под уведомлением открывает тот же пуш в три даты. Нижняя панель вкладок — вид, а не
переход. Панель «Действия» справа, клавиши <kbd>0</kbd>–<kbd>7</kbd> и параметр адреса
`journey.html?step=N` дублируют те же переходы и остаются пультом для показа; переход изнутри
телефона переписывает адрес (`history.replaceState`), поэтому панель, клавиши и адресная строка
показывают один экран. На `file://` замена адреса запрещена браузером — отказ гасится, переходы
работают. Дополнительные параметры: `&ctx=changed` · `closed` — дата открытия пуша (22.06 или
01.07); `&sheet=why` — шторка «Почему это сообщение», `&sheet=purpose` — лист «Выберите назначение
перевода». <kbd>Esc</kbd> закрывает шторку, тап по затемнению и «×» — тоже.

| `?step=` | Экран | Что показывает |
|---|---|---|
| `0` | Пуш на блокировке | уведомление 09.06.2026, шаблон `level_stall`; строка «Открыть как» под ним и три кнопки панели открывают тот же пуш в три даты |
| `1` | Экран перевода | факт, курс действия a_T, мини-график 90 дней публикации, «Почему это сообщение», три способа перевода со сроком, лимитом и комиссией |
| `2` | Получатель | четыре поля формы: счёт списания, номер, банк, назначение (открывает лист с четырьмя пунктами) |
| `3` | Сумма | 30 000 ₽, пересчёт в сомони по курсу ЦБ, «Без комиссии» |
| `4` | Проверка перевода | к списанию, получатель получит, курс, комиссия 0 ₽ |
| `5` | Готово и вердикт | перевод подтверждён + карточка «Этот сигнал» с вердиктом по правилу 20 дней |
| `6` | Курс изменился | механика A: две цифры, разница в % и в ₽ на 30 000 ₽; первая строка говорит, выполняется ли факт |
| `7` | Напомним при новом сигнале | ветка B как действие внутри экрана A, а не второй пуш |

Развилка замкнута: с экрана `6` кнопка «Посмотреть курс сейчас» ведёт на экран `1` с текущим курсом
и пересчитанной суммой (`?step=1&ctx=closed` — тот же путь с курсом 01.07), «Напомнить при новом
сигнале» — на экран `7`. Нижняя панель вкладок видна на экранах 1–5, активна «Платежи».

### Откуда числа

Придуманы только четверо: получатель, банк, назначение и счёт списания — клиентских данных в проекте
нет. Назначение выбрано из списка приложения («Безвозмездный перевод на текущие расходы»), счёт
списания назван без чисел — только «Текущий счёт ··0000».

- **Пуш** — `reports/latest/stream_decisions.csv`, коридор TJS, `decision=sent`, 09.06.2026: индикатор
  `level`, курс действия 7,66882 ₽ за 1 сомони, `pct_rank` 0,0417, окно 120 дней публикации, снижение
  остановилось 14 дней назад. Хеш прогона `c6a7e90` — из `reports/latest/provenance.json`.
- **Заголовок и текст уведомления** — `fxmoment.texts.library.render`, шаблон `level_stall`; прогон
  `fxmoment check-texts --title … --body …` на нём чист.
- **Курсы на даты открытия** — `data/raw/cbr_daily.csv`, снимок ЦБ РФ 03.09.2026: 22.06.2026 — 7,93983 ₽
  (+3,5 %), 01.07.2026 — 8,43411 ₽ (+10,0 %). Процентили в окне 120 дней: 0,2833 и 0,8667 — отсюда
  «ниже, чем в 72 % дней» и «выше, чем в 87 % дней».
- **Ряд мини-графика** — тот же CSV, коридор TJS, 105 значений курса действия с 29.01.2026 по 01.07.2026;
  на экране показаны последние 90 дней публикации до даты открытия. Линия вниз — сомони дешевеет для
  отправителя; точка — курс на дату открытия.
- **Сумма 30 000 ₽** — типовой перевод из `docs/product/customer-journey.md`, «Юнит-экономика перевода
  30 000 ₽ раз в месяц»: +85 бп и 254 ₽ с перевода в окнах, где пуш сдвинул перевод. Получатель получит
  3 911,94 сомони — расчёт по официальному курсу ЦБ РФ, без комиссии и спреда; это сказано и на экране,
  и в дисклеймере.
- **Вердикт сигнала** на экране `5` считает `verdict()` — то же правило, что в `index.html` и в
  `labels.hit_buy_now(mode="mean")` из ADR-0003: средний курс следующих 20 дней публикации против курса
  пуша с допуском 25 бп. Порог — 7,64965 ₽. На дату экрана (09.06.2026) горизонт не закрылся, поэтому
  бейдж показывает «ещё идёт», а вердикт «сбылся» стоит отдельной строкой с датой снимка: средний курс
  этих 20 дней 8,09146 ₽, известен 08.07.2026. Задним числом вердикт не подставляется.

### Компоненты и токены

Значения токенов сняты с Альфа-Онлайн (web.alfabank.ru, мобильная вёрстка 375 × 812, тёмная тема) и
совпадают с переменными скомпилированного бандла `core-components-styles`; имя переменной приложения
стоит в комментарии рядом с каждым токеном в `:root` файла.

Столбец «Живой экран» отмечает строки, где значение снято с самого приложения 06.09.2026: путь
«Главный → Быстрые переводы → За рубеж → страна → способ → форма перевода», тёмная тема. Там, где
столбец пуст, токен взят из дизайн-системы.

| Компонент прототипа | Компонент дизайн-системы / элемент приложения | Откуда токен | Живой экран Альфа-Онлайн 06.09 |
|---|---|---|---|
| Карточка уведомления на блокировке | `notification` | фон `#1c1c1e` — `color-dark-base-bg-secondary`; радиус 20 px — `--border-radius-20` | — |
| Навигационная строка экрана | `navigation-bar` | круглая иконка-кнопка 48 px, фон `rgba(255,255,255,.04)`; заголовок ушёл из строки в экран | ✅ форма перевода и модалка способа |
| Заголовки экранов | `typography` (`headline`, `title`) | форма — 26 px/500, line-height 32, letter-spacing −.25; списочный экран — 30 px/700; модалка способа — 32 px/500 Styrene UI с фолбэком на Alfa Interface Sans 500 | ✅ «Перевод по номеру телефона за рубеж», «За рубеж», «Перевод в Таджикистан» |
| Нижняя панель вкладок | `tab-bar` + `tab-bar/components/tab` | активная вкладка на плашке `#1c1c1e` радиусом 16 px, подпись 14 px; неактивная — `color-dark-text-secondary` | ✅ подпись 14 px и плашка вместо подсветки цветом |
| Поля формы (счёт списания, получатель, банк, назначение) | `input`, `select` (`form-control`) | карточка `rgba(214,214,229,.07)` — `color-dark-specialbg-secondary-transparent`, радиус 10 px — `--border-radius-10`, высота 72 px, отступы 16 px; подпись 14 px вторичным над значением 16 px/500; шеврон ▾ у селектов | ✅ все четыре поля |
| Карточки способа перевода | `pure-cell` (`content`) на карточке `#1c1c1e` | радиус 16 px, название 16 px/500, подзаголовки 13 px вторичным, тег-пилюля `#3193fc` — `color-dark-status-info`; иллюстрация 132 × 132 с CDN приложения | ✅ «По номеру телефона / карты / счёта» со сроком, лимитом и комиссией |
| Ячейки списка (проверка, сигналы) | `pure-cell` (`list`) | вертикальный ритм `--gap-12`/`--gap-8`; граница `#27272a` — `color-dark-border-secondary`; подложка иконки `rgba(214,214,229,.07)` | — |
| Главная кнопка «Продолжить», «Перевести», «Посмотреть курс сейчас» | `button`, вид `primary`, размер `l` | фон `#f2f3f5` — `--button-primary-base-bg-color` → `color-dark-accent-secondary`; текст `rgba(3,3,6,.88)` — `color-dark-text-primary-inverted`; высота 56 px, радиус 16 px, 16 px/500 | ✅ светлая кнопка в тёмной теме |
| Вторичная кнопка «Напомнить при новом сигнале» | `button`, вид `secondary` | фон `rgba(214,214,229,.07)` — `color-dark-specialbg-secondary-transparent`, тот же радиус 16 px | ✅ прозрачная плашка |
| Поле суммы | `amount-input` (`amount`) | карточка поля формы, подпись 14 px, значение 34 px/500; каретка — акцент | ✅ карточка и подпись, значение крупнее из-за экрана-одиночки |
| Шторка «Почему это сообщение» и лист назначения | `bottom-sheet` | фон `#1c1c1e`, радиус 20 px — `--border-radius-20`; заголовок 20 px/700, «×» в круге 32 px; пункты с расшифровкой 13 px вторичным | ✅ лист «Выберите назначение перевода», четыре пункта |
| Бейджи «+3,5 %», «ещё идёт», «сбылся» | `status-badge` | `#ff4837` — `color-dark-status-negative`; `#17d055` — `color-dark-status-positive` | — |
| Карточка факта и мини-график | `chart` (форма), карточка `system-message` | линия `color-dark-text-secondary`, точка и заливка — акцент | — |
| Типографика | `typography` / `typography-vars` | `"Alfa Interface Sans", system-ui, …` — `--font-family-alfasans`; ячейки и кнопки 16 px/500, подписи и подсказки 14 px | ✅ подсказки и подписи полей 14 px |
| Отступы и радиусы | `vars/src/gaps.css`, `vars/src/border-radius.css` | `--gap-4…24`; радиусы 8 / 10 / 12 / 16 / 20 / 24 px | ✅ 10 px у полей, 16 px у кнопок и карточек способа |

Одно расхождение названо здесь, чтобы его не искали в коде: **главная кнопка в тёмной теме
приложения светлая**, а в прототипе она была красной — он брал `button` вида `accent`
(`--button-accent-base-bg-color` → `color-dark-accent-primary`, `#f83a2a`). Взяли как в приложении:
вид `primary`, `--button-primary-base-bg-color` → `color-dark-accent-secondary`, то есть `#f2f3f5`
(`vars/src/colors-bluetint.css:8`). Красный остался там, где он и есть в приложении: логотип пуша,
теги и статусы.

Иллюстрации способов перевода (`transfer-type-groups/{phone,card,account}.png`, 132 × 132) не
копируются в репозиторий, как и файлы шрифта: они подгружаются с CDN приложения, а офлайн место
132 × 132 остаётся пустым и вёрстку не двигает.

Подсказка под назначением набрана в настоящем времени («Назначение знает только банк получателя»),
хотя в приложении стоит «Его будет знать только банк получателя»: будущее время запрещено
инвариантом 6 и снимается прогоном `fxmoment check-texts`.

Источник дизайна согласован: 💬 Даниил, 03.09.2026 — «дизайн Альфы я предлагаю спарсить с их Web PWA
в последствии, тем более это разрешили сами сотрудники Альфы» и «Супер, берём компоненты тогда.
Вместе с PWA» (`docs/product/star-task.md`, «Условие для старта»). Дизайн-система банка
[`core-ds/core-components`](https://github.com/core-ds/core-components) открыта под лицензией MIT;
из неё взяты разметка и правила компонентов, переписанные на чистый HTML/CSS без React, и значения
переменных. Ни файлов шрифта, ни иконок, ни кода библиотеки в репозиторий не копируется — иконки
нарисованы SVG-путями в самом файле.

### Что здесь 🧩, а не решение

- Даты открытия (22.06 и 01.07), время на экранах, порядок экранов формы и раскладка цифровой
  клавиатуры — выбор агента.
- Получатель, банк и назначение вымышлены; они же стоят в `index.html`. Счёт списания назван без
  суммы и без реального номера.
- Сроки, лимиты и комиссии на карточках способов перевода списаны с живого экрана 06.09 и относятся
  к переводу в Таджикистан; на других коридорах приложение показывает свои.
- Разбиение формы приложения на три экрана (получатель → сумма → проверка) — выбор агента: в
  приложении все поля лежат на одном экране.
- Названия и иконки вкладок нижней панели повторяют приложение по составу, но нарисованы заново.
- Экран `7` назван «Напомним при новом сигнале»: слово о нашем действии, а не о курсе. Тело экрана
  прогнано `fxmoment check-texts` — чисто; формулировка «по прогнозу» из первой редакции снята,
  потому что чекер ловит основу слова и не понимает отрицания.
